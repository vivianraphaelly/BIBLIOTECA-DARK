{
  "rules": {
    "users": {
      "$userKey": {
        ".read": true,
        ".write": "!data.exists()",
        ".validate": "newData.hasChildren(['name', 'email', 'salt', 'hash'])"
      }
    }
  }
}
